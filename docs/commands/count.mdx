---
title: "vibium count"
sidebar_label: count
description: "Count matching elements"
---

# vibium count

Count matching elements.

:::info[Generated reference]
Derived from `vibium help count` at **vibium v26.8.21**. The examples below are the
command's own built-in samples, not captured terminal output. Pages marked
*Verified* instead carry output from a live browser run.
:::

## Synopsis

```
vibium count [selector]
```

## Examples

```sh
vibium count "a"
# Print number of links on the page

vibium count "li.item"
# Count list items
```

## See also

- [Command reference](/docs/commands) — all commands, grouped by purpose.
- [Finding elements](/docs/commands#finding-elements) — related commands.
