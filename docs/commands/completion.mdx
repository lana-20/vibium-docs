---
title: "vibium completion"
sidebar_label: completion
description: "Generate a shell completion script."
---

# vibium completion

Generate a shell completion script.

:::info[Generated reference]
Derived from `vibium help completion` at **vibium v26.8.21**. The examples below are the
command's own built-in samples, not captured terminal output. Pages marked
*Verified* instead carry output from a live browser run.
:::

zsh — source it directly, or install it on your fpath:

  source &lt;(vibium completion zsh)
  vibium completion zsh &gt; "$&#123;fpath[1]&#125;/_vibium"

bash:

  source &lt;(vibium completion bash)

## Synopsis

```
vibium completion [bash|zsh|fish|powershell]
```

## Examples

```sh
vibium completion zsh
# → #compdef vibium
#   (( $+functions[compdef] )) || { autoload -Uz compinit && compinit -u; }
#   compdef _vibium vibium
#   ...
```

## See also

- [Command reference](/docs/commands) — all commands, grouped by purpose.
- [Meta](/docs/commands#meta) — related commands.
