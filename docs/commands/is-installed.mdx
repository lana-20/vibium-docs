---
title: "vibium is-installed"
sidebar_label: is-installed
description: "Check if the selected browser is installed (exit 0 = yes, exit 1 = no)"
---

# vibium is-installed

Check if the selected browser is installed (exit 0 = yes, exit 1 = no).

:::info[Generated reference]
Derived from `vibium help is-installed` at **vibium v26.8.21**. The examples below are the
command's own built-in samples, not captured terminal output. Pages marked
*Verified* instead carry output from a live browser run.
:::

## Synopsis

```
vibium is-installed
```

## See also

- [Command reference](/docs/commands) — all commands, grouped by purpose.
- [Browser & session](/docs/commands#browser--session) — related commands.
