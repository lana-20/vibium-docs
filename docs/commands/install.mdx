---
title: "vibium install"
sidebar_label: install
description: "Download the selected browser (Chrome for Testing by default)"
---

# vibium install

Download the selected browser (Chrome for Testing by default).

:::info[Generated reference]
Derived from `vibium help install` at **vibium v26.8.21**. The examples below are the
command's own built-in samples, not captured terminal output. Pages marked
*Verified* instead carry output from a live browser run.
:::

## Synopsis

```
vibium install
```

## Examples

```sh
vibium install
# Installing Chrome for Testing v139.0.7258.68...

vibium install --engine firefox
# Installing Firefox v153.0.3 (release channel)...

vibium install --engine firefox --channel beta
# Installing Firefox v154.0b6 (beta channel)...
```

## See also

- [Command reference](/docs/commands) — all commands, grouped by purpose.
- [Browser & session](/docs/commands#browser--session) — related commands.
