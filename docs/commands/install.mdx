---
title: "vibium install"
sidebar_label: install
description: "Download the selected browser"
---

# vibium install

Download the browser vibium will drive.

:::tip[Verified against a live browser]
Every example on this page is real terminal output captured from **vibium
v26.8.21** on macOS. Paths and ids will differ on your machine.
:::

## Synopsis

```
vibium install
```

## Flags

This command takes no flags of its own. Global flags apply — see
[Global flags](/docs/global-flags).

## Installing

`install` is idempotent — running it when the browser is present re-reports the
existing install rather than downloading again:

```sh
$ vibium install
Chrome for Testing v152.0.7977.54 already installed.
Installation complete!
Chrome: /Users/lanabegunova/Library/Caches/vibium/chrome-for-testing/152.0.7977.54/Google Chrome for Testing.app/Contents/MacOS/Google Chrome for Testing
Chromedriver: /Users/lanabegunova/Library/Caches/vibium/chrome-for-testing/152.0.7977.54/chromedriver
Version: 152.0.7977.54
```

Note that `Installation complete!` is printed in **both** cases, so it does not
tell you whether anything was downloaded — the line above it does.

Chrome for Testing is the default; `--engine firefox` installs Firefox, and
`--channel beta` selects the beta channel (Firefox only).

Commands launch the browser on demand, so `install` is only needed to
pre-populate the cache — in a Docker build or a CI image, for instance.

:::warning[`--json` is ignored, on this version and on current `main`]
Output is identical with and without the flag. It belongs to the family reported
as [#451](https://github.com/VibiumDev/vibium/issues/451) — three members of
which were fixed after this release, but this one was not.

Verified still present on `main` as of 2026-09-01, so this is not a version gap
— upgrading does not fix it.
:::

## See also

- [`vibium is-installed`](/docs/commands/is-installed) — the check to gate this on.
- [`vibium paths`](/docs/commands/paths) — where it lands.
- [Command reference](/docs/commands) — all commands, grouped by purpose.
