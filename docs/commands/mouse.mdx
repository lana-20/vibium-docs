---
title: "vibium mouse"
sidebar_label: mouse
description: "Mouse control (click, move, down, up)"
---

# vibium mouse

Mouse control (click, move, down, up).

:::info[Generated reference]
Derived from `vibium help mouse` at **vibium v26.8.21**. The examples below are the
command's own built-in samples, not captured terminal output. Pages marked
*Verified* instead carry output from a live browser run.
:::

## Synopsis

```
vibium mouse
```

## Subcommands

| Subcommand | Description |
| --- | --- |
| [`vibium mouse click`](#vibium-mouse-click) | Click at coordinates or current position |
| [`vibium mouse down`](#vibium-mouse-down) | Press a mouse button down |
| [`vibium mouse move`](#vibium-mouse-move) | Move the mouse to coordinates |
| [`vibium mouse up`](#vibium-mouse-up) | Release a mouse button |

### `vibium mouse click`

Click at coordinates or current position.

```
vibium mouse click [x] [y]
```

| Flag | Argument | Description |
| --- | --- | --- |
| `--button` | `int` | Mouse button (0=left, 1=middle, 2=right) |

```sh
vibium mouse click 100 200
# Left click at (100, 200)

vibium mouse click 100 200 --button 2
# Right click at (100, 200)

vibium mouse click
# Left click at current position
```

### `vibium mouse down`

Press a mouse button down.

```
vibium mouse down
```

| Flag | Argument | Description |
| --- | --- | --- |
| `--button` | `int` | Mouse button (0=left, 1=middle, 2=right) |

```sh
vibium mouse down
# Press left mouse button

vibium mouse down --button 2
# Press right mouse button
```

### `vibium mouse move`

Move the mouse to coordinates.

```
vibium mouse move [x] [y]
```

```sh
vibium mouse move 100 200
# Move mouse to position (100, 200)
```

### `vibium mouse up`

Release a mouse button.

```
vibium mouse up
```

| Flag | Argument | Description |
| --- | --- | --- |
| `--button` | `int` | Mouse button (0=left, 1=middle, 2=right) |

```sh
vibium mouse up
# Release left mouse button

vibium mouse up --button 2
# Release right mouse button
```

## See also

- [Command reference](/docs/commands) — all commands, grouped by purpose.
- [Interacting](/docs/commands#interacting) — related commands.
