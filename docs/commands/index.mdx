---
title: "Command reference"
sidebar_label: Overview
sidebar_position: 0
description: Every vibium CLI command, grouped by purpose.
---

# Command reference

Every command the vibium binary exposes at **vibium v26.8.21** — all 67 of them, grouped by purpose.

:::note[How this reference is built]
Pages marked *Verified* carry real terminal output captured from a live
browser. The rest are generated from the binary's own `--help` text by
[`scripts/gen_pages.py`](https://github.com/lana-20/vibium-docs), so the
reference can be re-derived whenever vibium ships a new release.
:::

## Navigation

| Command | Description |
| --- | --- |
| [`vibium go`](/docs/commands/go) | Go to a URL and print page info |
| [`vibium back`](/docs/commands/back) *(Verified)* | Navigate back in browser history |
| [`vibium forward`](/docs/commands/forward) *(Verified)* | Navigate forward in browser history |
| [`vibium reload`](/docs/commands/reload) *(Verified)* | Reload the current page |
| [`vibium url`](/docs/commands/url) *(Verified)* | Get the current page URL |
| [`vibium title`](/docs/commands/title) *(Verified)* | Get the current page title |

## Mapping & references

| Command | Description |
| --- | --- |
| [`vibium map`](/docs/commands/map) *(Verified)* | Map interactive page elements with @refs |
| [`vibium diff`](/docs/commands/diff) | Compare current state vs previous |

## Finding elements

| Command | Description |
| --- | --- |
| [`vibium find`](/docs/commands/find) *(Verified)* | Find elements by CSS selector or semantic locator |
| [`vibium frame`](/docs/commands/frame) | Find a frame by name or URL substring |
| [`vibium frames`](/docs/commands/frames) | List all child frames (iframes) on the page |
| [`vibium count`](/docs/commands/count) | Count matching elements |

## Interacting

| Command | Description |
| --- | --- |
| [`vibium click`](/docs/commands/click) *(Verified)* | Click an element (optionally navigate to URL first) |
| [`vibium dblclick`](/docs/commands/dblclick) | Double-click an element |
| [`vibium fill`](/docs/commands/fill) | Clear an input field and type new text |
| [`vibium type`](/docs/commands/type) | Type text into an element (optionally navigate to URL first) |
| [`vibium press`](/docs/commands/press) | Press a key on a specific element or the focused element |
| [`vibium keys`](/docs/commands/keys) | Press a key or key combination |
| [`vibium check`](/docs/commands/check) | Check a checkbox or radio button |
| [`vibium uncheck`](/docs/commands/uncheck) | Uncheck a checkbox |
| [`vibium select`](/docs/commands/select) | Select an option in a &lt;select&gt; element |
| [`vibium hover`](/docs/commands/hover) | Hover over an element by CSS selector |
| [`vibium focus`](/docs/commands/focus) | Focus an element |
| [`vibium drag`](/docs/commands/drag) | Drag from one element to another |
| [`vibium upload`](/docs/commands/upload) | Set files on an input[type=file] element |
| [`vibium scroll`](/docs/commands/scroll) | Scroll the page or an element |
| [`vibium mouse`](/docs/commands/mouse) | Mouse control (click, move, down, up) |
| [`vibium highlight`](/docs/commands/highlight) | Highlight an element with a red outline for 3 seconds |

## Reading page state

| Command | Description |
| --- | --- |
| [`vibium text`](/docs/commands/text) | Get text content of the page or an element |
| [`vibium html`](/docs/commands/html) | Get HTML content of the page or an element |
| [`vibium attr`](/docs/commands/attr) | Get an HTML attribute value from an element |
| [`vibium value`](/docs/commands/value) | Get the current value of a form element |
| [`vibium a11y-tree`](/docs/commands/a11y-tree) | Get the accessibility tree of the current page |
| [`vibium is`](/docs/commands/is) | Check element state (visible, enabled, checked, actionable) |
| [`vibium content`](/docs/commands/content) | Replace the page HTML content |

## Capture

| Command | Description |
| --- | --- |
| [`vibium screenshot`](/docs/commands/screenshot) *(Verified)* | Capture a screenshot (optionally navigate to URL first) |
| [`vibium pdf`](/docs/commands/pdf) | Save page as PDF |
| [`vibium eval`](/docs/commands/eval) | Evaluate a JavaScript expression (optionally navigate to URL first) |
| [`vibium record`](/docs/commands/record) | Record browser sessions (screenshots and snapshots) |

## Waiting

| Command | Description |
| --- | --- |
| [`vibium wait`](/docs/commands/wait) | Wait for an element, URL, text, page load, or JS condition |
| [`vibium sleep`](/docs/commands/sleep) | Pause execution for a number of milliseconds |

## Browser & session

| Command | Description |
| --- | --- |
| [`vibium start`](/docs/commands/start) | Start a browser session. Without arguments, launches a local browser. |
| [`vibium stop`](/docs/commands/stop) | Stop the browser session |
| [`vibium daemon`](/docs/commands/daemon) | Manage the vibium daemon (background browser process) |
| [`vibium page`](/docs/commands/page) | Manage browser pages (new, close, switch) |
| [`vibium pages`](/docs/commands/pages) | List all open browser pages |
| [`vibium viewport`](/docs/commands/viewport) | Get or set the browser viewport size |
| [`vibium window`](/docs/commands/window) | Get or set the OS browser window size, position, or state |
| [`vibium install`](/docs/commands/install) | Download the selected browser (Chrome for Testing by default) |
| [`vibium is-installed`](/docs/commands/is-installed) | Check if the selected browser is installed (exit 0 = yes, exit 1 = no) |
| [`vibium paths`](/docs/commands/paths) | Print browser and cache paths |
| [`vibium storage`](/docs/commands/storage) | Export or restore browser state (cookies, localStorage, sessionStorage) |
| [`vibium cookies`](/docs/commands/cookies) | Manage browser cookies |
| [`vibium download`](/docs/commands/download) | Manage browser downloads |
| [`vibium dialog`](/docs/commands/dialog) | Handle browser dialogs (alert, confirm, prompt) |
| [`vibium geolocation`](/docs/commands/geolocation) | Override the browser geolocation |
| [`vibium media`](/docs/commands/media) | Override CSS media features |

## Agent integration

| Command | Description |
| --- | --- |
| [`vibium mcp`](/docs/commands/mcp) | Start the Model Context Protocol (MCP) server. |
| [`vibium add-skill`](/docs/commands/add-skill) | Install Vibium browser skill for Claude Code |
| [`vibium pipe`](/docs/commands/pipe) | Start vibium in pipe mode where protocol messages are exchanged |
| [`vibium serve`](/docs/commands/serve) | Start WebSocket proxy server for browser automation |

## Diagnostics

| Command | Description |
| --- | --- |
| [`vibium launch-test`](/docs/commands/launch-test) | Launch the selected browser and print BiDi session info |
| [`vibium bidi-test`](/docs/commands/bidi-test) | Launch browser, connect via BiDi, send session.status |
| [`vibium ws-test`](/docs/commands/ws-test) | Test WebSocket connection (type messages, see echoes) |

## Meta

| Command | Description |
| --- | --- |
| [`vibium version`](/docs/commands/version) | Print the version number |
| [`vibium help`](/docs/commands/help) | Help provides help for any command in the application. |
| [`vibium completion`](/docs/commands/completion) | Generate a shell completion script. |

## Conventions

- Arguments shown as `<x>` are required; `[x]` are optional.
- `@eN` always refers to a numeric element reference returned by
  [`map`](/docs/commands/map) or [`find`](/docs/commands/find).
- All commands share a single browser and daemon, so state persists
  across invocations.
- Output is plain text on stdout unless a `-o` flag names a file, or
  the global `--json` flag is set — see [Global flags](/docs/global-flags).

## Running without installing

Every command works through `npx`, with no global install:

```sh
npx -y vibium go https://example.com
npx -y vibium map
npx -y vibium screenshot -o page.png
```

For a session-wide alias:

```sh
alias vibium='npx -y vibium'
```
