---
title: "vibium uncheck"
sidebar_label: uncheck
description: "Uncheck a checkbox"
---

# vibium uncheck

Untick a checkbox, if it is currently ticked.

:::tip[Verified against a live browser]
Every example on this page is real terminal output captured from **vibium
v26.8.21**, run against this site's own
[playground fixture](pathname:///vibium-docs/fixtures/playground.html) — a fixed page served
from this repo, so the output cannot drift when someone else's markup changes.
Every example is reproducible against that URL exactly as shown.
:::

## Synopsis

```
vibium uncheck <selector>
```

## Flags

| Flag | Argument | Description |
| --- | --- | --- |
| `--timeout` | `timeout` | Max time to wait (default 30s) |

Global flags apply as well — see [Global flags](/docs/global-flags).

## Idempotent, like `check`

```sh
$ vibium check "#terms"
Checked #terms

$ vibium uncheck "#terms"
Unchecked #terms

$ vibium is checked "#terms"
false

$ vibium uncheck "#terms"
Already unchecked: #terms

$ vibium is checked "#terms"
false
```

Running it twice is safe: the second call reports `Already unchecked` and leaves
the box alone, where a second [`click`](/docs/commands/click) would tick it
again.

## See also

- [`vibium check`](/docs/commands/check) — the inverse, and the fuller writeup.
- [`vibium is checked`](/docs/commands/is) — read the state.
- [Command reference](/docs/commands) — all commands, grouped by purpose.
